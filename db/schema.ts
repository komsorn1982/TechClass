import { integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const students = sqliteTable("students", { id: integer("id").primaryKey({ autoIncrement: true }), authUserId: text("auth_user_id").notNull(), email: text("email").notNull(), username: text("username").notNull(), passwordHash: text("password_hash").notNull(), passwordSalt: text("password_salt").notNull(), studentCode: text("student_code").notNull(), firstName: text("first_name").notNull(), lastName: text("last_name").notNull(), gradeLevel: integer("grade_level").notNull(), classroom: text("classroom").notNull(), avatarKey: text("avatar_key"), createdAt: integer("created_at", { mode: "timestamp" }).notNull() }, t => [uniqueIndex("idx_students_auth_user_id").on(t.authUserId), uniqueIndex("idx_students_email").on(t.email), uniqueIndex("idx_students_username").on(t.username), uniqueIndex("idx_students_student_code").on(t.studentCode)]);
export const courses = sqliteTable("courses", { id: integer("id").primaryKey({ autoIncrement: true }), code: text("code").notNull(), name: text("name").notNull(), gradeLevel: integer("grade_level").notNull(), semester: integer("semester").notNull(), description: text("description").notNull(), lessonCount: integer("lesson_count").notNull().default(0) }, t => [uniqueIndex("idx_courses_code").on(t.code)]);
export const sessions = sqliteTable("sessions", { id: integer("id").primaryKey({ autoIncrement:true }), studentId: integer("student_id").notNull().references(() => students.id, { onDelete:"cascade" }), tokenHash: text("token_hash").notNull(), expiresAt: integer("expires_at", { mode:"timestamp" }).notNull(), createdAt: integer("created_at", { mode:"timestamp" }).notNull() }, t => [uniqueIndex("idx_sessions_token_hash").on(t.tokenHash)]);
