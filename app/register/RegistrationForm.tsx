"use client";import{FormEvent,useState}from"react";import{ProfileImageCropper}from"../components/ProfileImageCropper";
export function RegistrationForm(){const[state,setState]=useState<"idle"|"saving"|"done"|"error">("idle"),[avatar,setAvatar]=useState<Blob|null>(null);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("saving");const data=new FormData(e.currentTarget);if(avatar)data.set("avatar",avatar,"profile.jpg");const res=await fetch("/api/students",{method:"POST",body:data});setState(res.ok?"done":"error")}return <form className="register-form" onSubmit={submit}><ProfileImageCropper onSave={setAvatar}/><div className="field-row"><label>ชื่อ<input name="firstName" required/></label><label>นามสกุล<input name="lastName" required/></label></div><div className="field-row"><label>ระดับชั้น<select name="gradeLevel" required defaultValue=""><option value="" disabled>เลือกระดับชั้น</option><option value="1">มัธยมศึกษาปีที่ 1</option><option value="2">มัธยมศึกษาปีที่ 2</option><option value="3">มัธยมศึกษาปีที่ 3</option></select></label><label>ห้องเรียน<input name="classroom" required placeholder="เช่น 1/2"/></label></div><label>เลขที่<input name="studentCode" required inputMode="numeric" pattern="[0-9]{1,3}"/></label><div className="credential-section"><div><b>ข้อมูลสำหรับเข้าสู่ระบบ</b></div><label>Username<input name="username" required minLength={4} maxLength={24} pattern="[A-Za-z0-9._@-]+"/></label><label>Password<input name="password" required type="password" minLength={8} maxLength={72}/></label></div><button className="auth-submit" disabled={state==="saving"}>{state==="saving"?"กำลังบันทึก...":"ลงทะเบียนนักเรียน →"}</button>{state==="done"&&<p className="form-success">✓ ลงทะเบียนเรียบร้อยแล้ว</p>}{state==="error"&&<p className="form-error">กรุณาตรวจสอบข้อมูล หรือ Username ถูกใช้งานแล้ว</p>}</form>}
