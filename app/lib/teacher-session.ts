import{and,eq,gt}from"drizzle-orm";import{cookies}from"next/headers";import{getDb}from"../../db";import{ensureAuthSchema}from"../../db/initialize";import{teachers,teacherSessions}from"../../db/schema";import{hashSessionToken}from"./password";
export async function getCurrentTeacher(){const token=(await cookies()).get("techclass_teacher_session")?.value;if(!token)return null;await ensureAuthSchema();const[r]=await getDb().select({id:teachers.id,firstName:teachers.firstName,lastName:teachers.lastName,username:teachers.username,avatarKey:teachers.avatarKey}).from(teacherSessions).innerJoin(teachers,eq(teacherSessions.teacherId,teachers.id)).where(and(eq(teacherSessions.tokenHash,await hashSessionToken(token)),gt(teacherSessions.expiresAt,new Date()))).limit(1);return r??null}
