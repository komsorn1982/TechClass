import{redirect}from"next/navigation";import{Header}from"../../components/Header";import{getCurrentTeacher}from"../../lib/teacher-session";import{TeacherProfileEditor}from"./TeacherProfileEditor";export const dynamic="force-dynamic";
export default async function Page(){const teacher=await getCurrentTeacher();if(!teacher)redirect("/teacher/login");return <main><Header/><section className="profile-shell wrap"><div className="profile-heading"><span className="mini">บัญชีผู้สอน</span><h1>ข้อมูลโปรไฟล์ครู</h1><p>ดูและแก้ไขข้อมูลส่วนตัว รูปโปรไฟล์ และบัญชีเข้าสู่ระบบ</p></div><TeacherProfileEditor teacher={{...teacher,avatarUrl:teacher.avatarKey?"/api/teacher/avatar":null}}/></section></main>}
