"use client";import{FormEvent,useState}from"react";
export function TeacherResetForm(){const[state,setState]=useState<"idle"|"saving"|"done"|"error"|"mismatch">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget,data=Object.fromEntries(new FormData(form))as Record<string,string>;if(data.password!==data.confirmPassword){setState("mismatch");return}setState("saving");const r=await fetch("/api/teacher/reset-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});setState(r.ok?"done":"error");if(r.ok)form.reset()}return <form className="register-form reset-form" onSubmit={submit}><label>Username<input name="username" required minLength={4} maxLength={24} pattern="[A-Za-z0-9._@-]+"/></label><div className="identity-box"><b>ยืนยันข้อมูลคุณครู</b><div className="field-row"><label>ชื่อ<input name="firstName" required/></label><label>นามสกุล<input name="lastName" required/></label></div></div><label>รหัสผ่านใหม่<input name="password" required type="password" minLength={8} maxLength={72}/></label><label>ยืนยันรหัสผ่านใหม่<input name="confirmPassword" required type="password" minLength={8} maxLength={72}/></label><button className="auth-submit" disabled={state==="saving"}>{state==="saving"?"กำลังเปลี่ยนรหัสผ่าน...":"เปลี่ยนรหัสผ่าน →"}</button>{state==="done"&&<div className="reset-success">✓ เปลี่ยนรหัสผ่านแล้ว <a href="/teacher/login">เข้าสู่ระบบครู</a></div>}{state==="mismatch"&&<p className="form-error">รหัสผ่านใหม่ทั้งสองช่องไม่ตรงกัน</p>}{state==="error"&&<p className="form-error">ข้อมูลคุณครูไม่ถูกต้อง กรุณาตรวจสอบอีกครั้ง</p>}</form>}
