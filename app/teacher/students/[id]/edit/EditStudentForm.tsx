"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";
type Student={id:number;number:string;firstName:string;lastName:string;grade:number;classroom:string};
export function EditStudentForm({student}:{student:Student}){const router=useRouter(),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function save(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const data=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch(`/api/teacher/students/${student.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(data)});setBusy(false);if(response.ok){router.push("/teacher/students");router.refresh()}else setError((await response.json()).error??"บันทึกไม่สำเร็จ")}return <form className="profile-form edit-student-form" onSubmit={save}><section><div className="profile-section-head"><span>01</span><div><h2>ข้อมูลนักเรียน</h2><p>แก้ไขข้อมูลที่ครูใช้จัดห้องและเรียงรายชื่อ</p></div></div><div className="field-row"><label>ชื่อ<input name="firstName" defaultValue={student.firstName} required/></label><label>นามสกุล<input name="lastName" defaultValue={student.lastName} required/></label></div><div className="field-row"><label>เลขที่<input name="number" defaultValue={student.number} inputMode="numeric" pattern="[0-9]{1,3}" required/></label><label>ระดับชั้น<select name="grade" defaultValue={student.grade}>{[1,2,3].map(n=><option value={n} key={n}>มัธยมศึกษาปีที่ {n}</option>)}</select></label></div><label>ห้องเรียน<input name="classroom" defaultValue={student.classroom} required/></label></section><div className="profile-save"><div>{error&&<p className="form-error">{error}</p>}</div><button className="auth-submit" disabled={busy}>{busy?"กำลังบันทึก...":"บันทึกข้อมูลนักเรียน"}</button></div></form>}
