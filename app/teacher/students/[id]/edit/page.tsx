import Link from "next/link";import {eq} from "drizzle-orm";import {notFound,redirect} from "next/navigation";import {getDb} from "../../../../../db";import {ensureAuthSchema} from "../../../../../db/initialize";import {students} from "../../../../../db/schema";import {Header} from "../../../../components/Header";import {getCurrentTeacher} from "../../../../lib/teacher-session";import {EditStudentForm} from "./EditStudentForm";
export default async function EditStudentPage({params}:{params:Promise<{id:string}>}){if(!await getCurrentTeacher())redirect("/teacher/login");await ensureAuthSchema();const{id}=await params;const[student]=await getDb().select({id:students.id,number:students.studentCode,firstName:students.firstName,lastName:students.lastName,grade:students.gradeLevel,classroom:students.classroom}).from(students).where(eq(students.id,Number(id))).limit(1);if(!student)notFound();return <><Header/><main className="profile-shell wrap"><Link className="back-link" href="/teacher/students">← กลับไปรายชื่อนักเรียน</Link><div className="profile-heading"><span className="mini">TEACHER ONLY</span><h1>แก้ไขข้อมูลนักเรียน</h1><p>{student.firstName} {student.lastName} · เลขที่ {student.number}</p></div><EditStudentForm student={student}/></main></>}
