"use client";import{FormEvent,useState}from"react";
export function TeacherRegisterForm(){const[state,setState]=useState<"idle"|"saving"|"done"|"error">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("saving");const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/teacher/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});setState(r.ok?"done":"error")}return <form className="register-form" onSubmit={submit}><div className="field-row"><label>ชื่อ<input name="firstName" required placeholder="ชื่อจริง"/></label><label>นามสกุล<input name="lastName" required placeholder="นามสกุล"/></label></div><div className="credential-section"><div><b>ข้อมูลบัญชีครู</b><small>ใช้สำหรับเข้าสู่ระบบ TechClass</small></div><label>Username<input name="username" required minLength={4} maxLength={24} pattern="[A-Za-z0-9._@-]+" placeholder="เช่น teacher.somchai"/></label><label>Password<input name="password" required type="password" minLength={8} maxLength={72} placeholder="อย่างน้อย 8 ตัวอักษร"/></label></div><button className="auth-submit" disabled={state==="saving"}>{state==="saving"?"กำลังลงทะเบียน...":"ลงทะเบียนครู →"}</button>{state==="done"&&<div className="reset-success">✓ ลงทะเบียนแล้ว <a href="/teacher/login">เข้าสู่ระบบ</a></div>}{state==="error"&&<p className="form-error">กรุณาตรวจสอบข้อมูล หรือ Username ถูกใช้งานแล้ว</p>}</form>}
