"use client";import Link from"next/link";import{FormEvent,useState}from"react";
export function TeacherLoginForm(){const[state,setState]=useState<"idle"|"loading"|"error">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("loading");const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/teacher/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(r.ok)window.location.href="/";else setState("error")}return <form className="login-form" onSubmit={submit}><label>Username<input name="username" required pattern="[A-Za-z0-9._@-]+"/></label><label>Password<input name="password" required type="password"/></label><div className="login-help"><Link href="/teacher/forgot-password">ลืมรหัสผ่าน?</Link></div><button className="auth-submit" disabled={state==="loading"}>{state==="loading"?"กำลังเข้าสู่ระบบ...":"เข้าสู่ระบบครู →"}</button>{state==="error"&&<p className="form-error">Username หรือ Password ไม่ถูกต้อง</p>}</form>}
