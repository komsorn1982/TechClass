"use client";import{FormEvent,useState}from"react";import{ProfilePhotoEditor}from"./ProfilePhotoEditor";
type Person={firstName:string;lastName:string;username:string;avatarUrl:string|null;studentCode?:string;gradeLevel?:number;classroom?:string};
export function EditableAccountProfile({person,profileEndpoint,avatarEndpoint,loginPath,teacher=false}:{person:Person;profileEndpoint:string;avatarEndpoint:string;loginPath:string;teacher?:boolean}){const[state,setState]=useState<"idle"|"saving"|"done"|"error">("idle"),[message,setMessage]=useState("");async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("saving");const data=Object.fromEntries(new FormData(e.currentTarget));const response=await fetch(profileEndpoint,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(data)});const result=await response.json()as{error?:string;passwordChanged?:boolean};if(!response.ok){setMessage(result.error||"บันทึกข้อมูลไม่ได้");setState("error");return}if(result.passwordChanged){alert("เปลี่ยนรหัสผ่านแล้ว กรุณาเข้าสู่ระบบใหม่");window.location.href=loginPath;return}setMessage("บันทึกข้อมูลเรียบร้อยแล้ว");setState("done");setTimeout(()=>window.location.reload(),700)}return <div className="profile-layout"><ProfilePhotoEditor initialAvatar={person.avatarUrl} initial={person.firstName.charAt(0)} endpoint={avatarEndpoint} teacher={teacher}/><form className="profile-form" onSubmit={save}><section><div className="profile-section-head"><span>01</span><div><h2>{teacher?"ข้อมูลคุณครู":"ข้อมูลนักเรียน"}</h2><p>ข้อมูลที่ใช้แสดงในระบบ TechClass</p></div></div><div className="field-row"><label>ชื่อ<input name="firstName" required defaultValue={person.firstName}/></label><label>นามสกุล<input name="lastName" required defaultValue={person.lastName}/></label></div>{!teacher&&<><div className="field-row"><label>ระดับชั้น<select name="gradeLevel" required defaultValue={person.gradeLevel}><option value="1">มัธยมศึกษาปีที่ 1</option><option value="2">มัธยมศึกษาปีที่ 2</option><option value="3">มัธยมศึกษาปีที่ 3</option></select></label><label>ห้องเรียน<input name="classroom" required defaultValue={person.classroom}/></label></div><label>เลขที่<input name="studentCode" required pattern="[0-9]{1,3}" defaultValue={person.studentCode}/></label></>}</section><section><div className="profile-section-head"><span>02</span><div><h2>บัญชีเข้าสู่ระบบ</h2><p>แก้ไข Username หรือ Password</p></div></div><label>Username<input name="username" required minLength={4} maxLength={24} pattern="[A-Za-z0-9._@-]+" defaultValue={person.username}/></label><label>รหัสผ่านปัจจุบัน<input name="currentPassword" type="password" autoComplete="current-password" placeholder="จำเป็นเมื่อเปลี่ยน Username หรือ Password"/></label><div className="field-row"><label>Password ใหม่<input name="newPassword" type="password" minLength={8} maxLength={72} autoComplete="new-password" placeholder="เว้นว่างหากไม่เปลี่ยน"/></label><label>ยืนยัน Password ใหม่<input name="confirmPassword" type="password" minLength={8} maxLength={72} autoComplete="new-password"/></label></div></section><div className="profile-save"><div>{state==="done"&&<p className="form-success">✓ {message}</p>}{state==="error"&&<p className="form-error">{message}</p>}</div><button className="auth-submit" disabled={state==="saving"}>{state==="saving"?"กำลังบันทึก...":"บันทึกข้อมูล →"}</button></div></form></div>}
