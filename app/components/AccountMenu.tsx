"use client";
import Link from "next/link";
export function AccountMenu({name,avatarUrl}:{name:string;avatarUrl:string|null}){async function logout(){await fetch("/api/auth/logout",{method:"POST"});window.location.href="/"}return <div className="account-menu"><Link className="avatar-button" href="/profile" aria-label="เปิดข้อมูลโปรไฟล์">{avatarUrl?<img src={avatarUrl} alt="รูปโปรไฟล์"/>:<span>{name.charAt(0)}</span>}<i>›</i></Link><Link className="account-name" href="/profile"><small>นักเรียน</small><strong>{name}</strong></Link><button className="logout-button" onClick={logout}>Logout</button></div>}
