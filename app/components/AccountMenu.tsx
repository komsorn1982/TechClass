"use client";import Link from"next/link";
export function AccountMenu({name,avatarUrl,profileUrl,role,logoutUrl}:{name:string;avatarUrl:string|null;profileUrl:string;role:string;logoutUrl:string}){async function logout(){await fetch(logoutUrl,{method:"POST"});window.location.href="/"}return <div className="account-menu"><Link className="avatar-button" href={profileUrl} aria-label="เปิดข้อมูลโปรไฟล์">{avatarUrl?<img src={avatarUrl} alt="รูปโปรไฟล์"/>:<span>{name.charAt(0)}</span>}<i>›</i></Link><Link className="account-name" href={profileUrl}><small>{role}</small><strong>{name}</strong></Link><button className="logout-button" onClick={logout}>Logout</button></div>}
