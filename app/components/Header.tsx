import Link from "next/link";import { getCurrentStudent } from "../lib/session";import { AccountMenu } from "./AccountMenu";
export async function Header(){const student=await getCurrentStudent();return <nav className="nav wrap" aria-label="เมนูหลัก"><Link className="brand" href="/"><span className="brandmark">TC</span><span>TECH<span>CLASS</span></span></Link><div className="navlinks"><Link href="/">หน้าแรก</Link><Link href="/#experience">บรรยากาศการเรียน</Link><Link href="/#about">เกี่ยวกับเรา</Link></div>{student?<AccountMenu name={`${student.firstName} ${student.lastName}`} avatarUrl={student.avatarKey?"/api/profile/avatar":null}/>:<div className="account-actions"><Link className="login-link" href="/login">เข้าสู่ระบบ</Link><Link className="navbutton" href="/register">ลงทะเบียน <span>→</span></Link></div>}</nav>}
