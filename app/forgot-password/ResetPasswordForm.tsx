"use client";import{FormEvent,useState}from"react";
export function ResetPasswordForm(){const[state,setState]=useState<"idle"|"saving"|"done"|"error"|"mismatch">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget,data=Object.fromEntries(new FormData(form))as Record<string,string>;if(data.password!==data.confirmPassword){setState("mismatch");return}setState("saving");const response=await fetch("/api/auth/reset-password",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});setState(response.ok?"done":"error");if(response.ok)form.reset()}return <form className="register-form reset-form" onSubmit={submit}><label>Username<input name="username" required minLength={4} maxLength={24} pattern="[A-Za-z0-9._@-]+" autoComplete="username" placeholder="Username"/></label><div className="identity-box"><b>ยืนยันข้อมูลนักเรียน</b><div className="field-row"><label>ชื่อ<input name="firstName" required placeholder="ชื่อจริง"/></label><label>นามสกุล<input name="lastName" required placeholder="นามสกุล"/></label></div><div className="field-row"><label>ระดับชั้น<select name="gradeLevel" required defaultValue=""><option value="" disabled>เลือกระดับชั้น</option><option value="1">มัธยมศึกษาปีที่ 1</option><option value="2">มัธยมศึกษาปีที่ 2</option><option value="3">มัธยมศึกษาปีที่ 3</option></select></label><label>ห้องเรียน<input name="classroom" required placeholder="เช่น 1/2"/></label></div><label>เลขที่<input name="studentCode" required inputMode="numeric" pattern="[0-9]{1,3}" placeholder="เช่น 12"/></label></div><label>รหัสผ่านใหม่<input name="password" required type="password" minLength={8} maxLength={72} autoComplete="new-password" placeholder="อย่างน้อย 8 ตัวอักษร"/></label><label>ยืนยันรหัสผ่านใหม่<input name="confirmPassword" required type="password" minLength={8} maxLength={72} autoComplete="new-password" placeholder="กรอกรหัสผ่านใหม่อีกครั้ง"/></label><button className="auth-submit" disabled={state==="saving"}>{state==="saving"?"กำลังเปลี่ยนรหัสผ่าน...":"เปลี่ยนรหัสผ่าน →"}</button>{state==="done"&&<div className="reset-success">✓ เปลี่ยนรหัสผ่านแล้ว <a href="/login">เข้าสู่ระบบ</a></div>}{state==="mismatch"&&<p className="form-error">รหัสผ่านใหม่ทั้งสองช่องไม่ตรงกัน</p>}{state==="error"&&<p className="form-error">ข้อมูลนักเรียนไม่ถูกต้อง กรุณาตรวจสอบอีกครั้ง</p>}</form>}
