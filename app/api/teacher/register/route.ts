import{NextResponse}from"next/server";import{getDb}from"../../../../db";import{ensureAuthSchema}from"../../../../db/initialize";import{teachers}from"../../../../db/schema";import{hashPassword}from"../../../lib/password";
const U=/^[A-Za-z0-9._@-]{4,24}$/;export async function POST(r:Request){const b=await r.json()as Record<string,string>;if(!b.firstName||!b.lastName||!U.test(b.username||"")||(b.password?.length??0)<8)return NextResponse.json({error:"ข้อมูลไม่ถูกต้อง"},{status:400});try{await ensureAuthSchema();await getDb().insert(teachers).values({firstName:b.firstName,lastName:b.lastName,username:b.username.toLowerCase(),...await hashPassword(b.password),createdAt:new Date()});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"Username ถูกใช้แล้ว"},{status:409})}}
