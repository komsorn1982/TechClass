import{eq}from"drizzle-orm";import{cookies}from"next/headers";import{NextResponse}from"next/server";import{getDb}from"../../../../db";import{teacherSessions}from"../../../../db/schema";import{hashSessionToken}from"../../../lib/password";
export async function POST(){const token=(await cookies()).get("techclass_teacher_session")?.value;if(token)await getDb().delete(teacherSessions).where(eq(teacherSessions.tokenHash,await hashSessionToken(token)));const r=NextResponse.json({ok:true});r.cookies.set("techclass_teacher_session","",{httpOnly:true,sameSite:"lax",path:"/",maxAge:0});return r}
